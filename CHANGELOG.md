# Changelog

本项目按 [Semantic Versioning 2.0.0](https://semver.org/) 记录发布变化。日期使用项目维护时区（Asia/Shanghai）。

## [Unreleased]

## [0.3.5] - 2026-08-20

### Fixed

- 修复五连音候选使用 192 tick 网格却无法整除 480 tick 拍组时，将落在拍线上的尾音继续舍入到小节时间轴之外并触发“音符位置超出小节时间轴”的问题；候选网格现在必须完整闭合当前拍组，最后小节的书写时值另有边界保护。
- 修复最后一拍为了避开终止拍线而把不同起音的短音程连续音挤成同起双音的问题；尾部网格会先确认内部位置足够，再按前后顺序联合分配起音。

### Validation

- 新增可稳定复现旧错误的末拍五连音边界回归：旧版在弱起重组后于最后小节线报错，修复后 3/3 个音头均保留、无假和弦且所有时值为正。
- 重新转录并转换下载目录中的 `苍鸟.mp4`；默认模式保留 3908/3908 个音，稳定速度仍为 129.199 BPM。允许多连音时，简洁/均衡/忠实与四种最短音符设置共 12 组组合全部保持在末小节时间轴内。
- 对照参考谱 `Aoitori（苍鸟）（偶像大师ost）.pdf`：开头为完整 4/4 小节，尾声包含 2/4 至 4/4 的拍号切换、rubato 与 rit.。参考谱约 120 小节，而当前纯音频候选仍按 129.2 BPM / 182 小节解释并重组出半小节弱起，说明另有 3:2 节拍层级与片头静音判定问题；本补丁只修复确定的尾部越界，不通过凭空追加小节掩盖错误。

## [0.3.4] - 2026-08-20

### Added

- Librosa 现在同时提供稳定全局速度轨与自适应局部速度候选；转录起音网格负责选择候选，并用相邻拍周期连续性惩罚 4:3、3:2、半速/倍速层级的来回跳变。

### Changed

- 同一拍内 7 个及以上真实起音按高阶加花处理：使用能保留先后顺序的 32/64 分二元细网格近似，不强写 480 divisions 无法精确表达且容易制造休止符的 7/9 连音。
- 密集跑动改为按整拍联合量化起音列；原本同起音的真实和弦继续共用一个网格点，不同起音的短音程级进必须按前后关系落在递增网格点上。

### Fixed

- 修复攻击音抖动对齐把连续快速级进提前合并成短音程双音的问题；孤立双音、三音以上和弦和释放同步的微滚奏仍可正常对齐。
- 修复粗网格复杂度价格高于假和弦惩罚时，四音快速跑动、七音及九音加花被挤到同一时刻的问题；即使两次起音相距不足半个 64 分音符，也会在可打印范围内保留先后顺序。
- 修复自适应节拍轨在变速段偶发切换节拍层级、反而破坏原有稳定节奏的问题；只有连续且明显改善起音拟合的变速轨才会被采用。

### Validation

- 下载目录真实视频 `苍鸟.mp4` 保留 3908/3908 个音符；稳定候选以 129.199 BPM 胜出，抖动较大的自适应轨被连续性门禁拒绝。旧量化路径在快速上下文产生 20 组短音程假双音，修复后为 0。
- 同一视频检测到 13 个每拍含 7 个以上起音的高密度片段，全部保留原始起音列；最密片段保留 10/10 个不同起音。既有 `40869366033-1-192.mp4` 仍保持 4/4、一拍弱起相位 1.75 与 2960 个音符。

## [0.3.3] - 2026-08-20

### Changed

- 将音频自动三连音全曲门槛放宽到约 5%，避免短小但真实的三/六连音被过度改写为切分；局部候选仍须有至少三个真实起音、比率证据和明显优于二元网格的拟合。
- 自动识别的可见多连音现在必须至少有三个真实起音，且发声音多于休止符；证据不足但 MusicXML 回放需要的完整组保留为不显示括号、数字和补齐休止符的隐藏时序容器。
- 网页分别解释“简洁 / 均衡 / 忠实”的记谱整理强度与“经典出版 / 现代清晰 / 紧凑演奏”的视觉差异，并推荐“简洁 + 经典出版”。

### Fixed

- 修复 4/4 第一拍与第三拍低音强度接近时误选半小节相位的问题；真实视频回归由三拍弱起恢复为一拍弱起，首小节正确标记为不完全小节。
- 修复同起音、原始释放接近的八度与和弦在声部清理后出现不同时值、重复音柱和符头错位的问题，同时保留真正独立的复调释放。
- 修复仅有少量真实起音的大量休止符多连音被显示在谱面上的问题，并保持 MuseScore 时序与回读音头不变。

### Validation

- 真实视频 `40869366033-1-192.mp4` 回转保留 2960/2960 个音符，起音 F1 为 0.9939、平均误差为 0.02229 拍；统一 106 个八度/和弦音时值，显示 29 个有充分证据的多连音并隐藏 83 个技术时序组。
- 可见多连音中 0 个休止符占多数、0 个少于三个发声音；可见休止符 102，连音括号失衡 0，MuseScore 正常回读并导出 8 页 A4 PDF。

## [0.3.2] - 2026-08-20

### Changed

- 音频自动量化改为以起音位置作为节奏网格的主要证据，不再让模型释放时间的抖动推动三、五、六连音判定。
- 自动多连音只由真正的三连音网格投票启用，不再连带开放五连音；局部候选必须至少有三个真实起音，并显著优于最佳二分网格。
- 倚音按同声部的前音、候选音和目标音联合判断，只保留邻音回归或反向级进解决等装饰性关系；普通同向快速音和目标和弦前的同音重击保持逐音书写。

### Fixed

- 修复稀疏音符被休止符补齐为三连音、五连音或六连音括号的问题。
- 修复转录起音抖动把普通十六分音符级进误判为倚音的问题。

## [0.3.1] - 2026-08-20

### Added

- 网页新增作品信息区，可独立编辑乐谱标题、作者/编曲和导出文件名，并实时预览 MusicXML/PDF 下载名。
- MIDI、音视频、MusicXML/MXL 与 PDF 转换接口统一支持作者元数据和安全化的自定义输出文件名。

### Changed

- 选择新上传文件时，乐谱标题与默认导出名会同步切换为新文件名；作者保留以便批量处理同一作者作品。

### Fixed

- 修复 Audiveris MusicXML 多声部时间轴解析错误：`backup` 现在作用于小节共享游标，普通小节按拍号边界推进，单个破损小节不再造成后续累计漂移。
- 自动移除没有任何反复记号时的孤立结局线，避免横向括号误识别后让 MuseScore 跳过大段音符；严重小节越界会触发安全的语义重建。

## [0.3.0] - 2026-08-20

### Added

- 增加基于重复拍组节奏型的时值修复：相同起音结构互相校验短暂延长/缩短，孤立短音不再自动解释成跳音。
- 增加音频细网格证据门禁；只有同一拍内存在连续快速起音时，自动模式才允许 32/64 分音符网格。
- 增加语义版本同步测试、版本分级文档，并在网页、API 和 MusicXML 编码信息中标注版本。

### Changed

- 音视频时间轴回到 Librosa 节拍映射与稳定速度候选，不再用局部音符密度直接弯曲全曲速度。
- 跳音规范恢复为半槽短奏，并要求至少三次重复节奏位置或至少三个相邻短奏起音作为证据；孤立、成对短音不自动加点，同一和弦只输出一个跳音点。
- `tr` 仅用于 32 分音符或更快、且实际交替间隔足够短的二度交替；普通十六分音符保持逐音书写。
- 演奏缝隙只在合并后得到单一可记谱时值时才吸收，避免生成 150/210/270 tick 一类碎时值链。

### Fixed

- 修复音符 IOI 被误当成固定十六分音符所造成的节奏拉伸、伪三/五连音和错误速度坡道。
- 修复速度事件以“四分音符位置”与“tick”混比，导致中后段速度被错误写成初始速度的问题。
- 修复和弦每个音头重复写跳音记号的问题。

## [0.1.0] - 2026-08-18

- 首个可运行版本：MIDI/音视频转录、MusicXML、MuseScore A4 PDF、双手/谱表/声部规划与质量报告。
